export const toggleMode = mode => ({
  type: "TOGGLE_MODE",
  mode
});

export const inputUserName = username => ({
  type: "INPUT_USERNAME",
  username
});

export const inputPassword = password => ({
  type: "INPUT_PASSWORD",
  password
});
